import { IStoreListingStyleProps, IStoreListingStyles } from "./StoreListing.types";

// unused placeholder props must starts with _ because of eslint rule, change it when used
export function getStyles(_props: IStoreListingStyleProps): IStoreListingStyles {
  return {};
}
