import { IOfferListingStyleProps, IOfferListingStyles } from "./OfferListing.types";

// unused placeholder props must starts with _ because of eslint rule, change it when used
export function getStyles(_props: IOfferListingStyleProps): IOfferListingStyles {
  return {};
}
